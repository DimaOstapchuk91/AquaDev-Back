description: 'Unauthorized error'
content:
  application/json:
    schema:
      type: object
      required:
        - status
        - message
        - details
      properties:
        status:
          type: integer
          example: 401
        message:
          type: string
          example: Authentication failed
        details:
          type: string
          example: Invalid or expired token
