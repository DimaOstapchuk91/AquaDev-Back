description: 'User not found'
content:
  application/json:
    schema:
      type: object
      required:
        - status
        - message
        - details
      properties:
        status:
          type: integer
          example: 404
        message:
          type: string
          example: Resource not found
        details:
          type: string
          example: The requested resource does not exist
